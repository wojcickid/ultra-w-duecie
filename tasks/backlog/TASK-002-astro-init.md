# TASK-002 — Inicjalizacja projektu Astro + Tailwind

## Status

backlog

## Owner

frontend-agent

## Dependencies

- Brak

## Description

Utworzyć szkielet projektu: Astro (TypeScript, tryb statyczny), Tailwind CSS, formatowanie i lint, skrypty `dev`/`build`/`preview`, podstawowa struktura katalogów, `lang="pl"`.

## Context

Patrz `docs/architecture.md` i `docs/ui.md`. Nie dodawać zależności bez powodu. Zależności przypiąć w lockfile.

## Acceptance criteria

- [ ] `npm run dev` i `npm run build` działają bez błędów.
- [ ] Skonfigurowano formatowanie i lint (skrypty w `package.json`).
- [ ] Strona startowa renderuje się z `lang="pl"`.
- [ ] `.gitignore` obejmuje artefakty budowy; brak sekretów w repozytorium.
- [ ] README zawiera krótką instrukcję uruchomienia lokalnego.

## Implementation notes

Optional notes added during implementation.

## Validation

### Tests

- Not run

### Review

- Not reviewed

## Outcome

Complete this section before moving the task to `done`.

- Summary:
- Tests:
- Important files:
- Commit:
- Follow-up tasks:
